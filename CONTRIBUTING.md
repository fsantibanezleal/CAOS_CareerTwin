# Contributing

Start with `Entry_point.md` and an issue. Branch from `develop`, keep changes scoped, add or update
tests and documentation, and run `scripts/verify` for your platform before opening a pull request.

Contributions must preserve tenant isolation, evidence provenance, deterministic score semantics and
the rule that agent-proposed canonical changes require explicit user approval. Never add real user
data or secrets to fixtures.

